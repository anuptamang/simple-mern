import Banner from '../../components/UI/Banner'
import { images } from '../../data/static'

const ContactBanner = () => {
  return <Banner heading={'Contact'} imageUrl={images.ImgContact} />
}

export default ContactBanner
